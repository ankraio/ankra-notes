{
  "name": "ankra-notes",
  "version": "1.0.0",
  "private": true,
  "description": "A small notes API: Express + PostgreSQL, with link previews from an external service.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
