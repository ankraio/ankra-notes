apiVersion: v1
kind: Service
metadata:
  name: ankra-notes
  labels:
    app.kubernetes.io/name: ankra-notes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ankra-notes
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
