apiVersion: apps/v1
kind: Deployment
metadata:
  name: ankra-notes-v2
  labels:
    app.kubernetes.io/name: ankra-notes-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ankra-notes-v2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ankra-notes-v2
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: ankra-registry-pull
      containers:
        - name: ankra-notes-v2
          image: registry.ankra.cloud/org-7b2fedb6-4d79-45fd-b71a-044a2cfddce7/ankra-notes:${{ ankra.image_tag }}
          imagePullPolicy: IfNotPresent
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ankra-notes-v2-db-app
                  key: uri
            - name: PREVIEW_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ankra-notes-v2-env
                  key: PREVIEW_API_KEY
                  optional: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 30
            periodSeconds: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
      volumes:
        - name: tmp
          emptyDir: {}
